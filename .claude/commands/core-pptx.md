# /core-pptx — Yönetim Sunumu Üret
# Kullanım: /core-pptx PAY-1234

.core/agents/orchestrator.agent.md ve .core/skills/output-formats/SKILL.md oku.

Ticket: $ARGUMENTS
core-output/$ARGUMENTS/ klasöründeki analiz dosyalarını oku.
Format 1 yapısını kullan → 8 slide yönetim sunumu (Türkçe).
Çıktı: core-output/$ARGUMENTS/sunum.pptx
Onay bekleme, direkt üret.
