---
agent: 'agent'
tools: ['search/codebase']
description: 'CORE — Yönetim sunumu üret: ticket analizinden PowerPoint-uyumlu slayt içeriği'
---

#file:.core/agents/pptx.agent.md dosyasını oku.
Tüm `<activation>`, `<workflow>`, `<rules>` bloklarını adım adım uygula.

Ticket ID: ${input:ticket}
