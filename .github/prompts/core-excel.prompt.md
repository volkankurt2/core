---
agent: 'agent'
tools: ['search/codebase']
description: 'CORE — Ticket için Excel etki matrisi üret'
---

#file:.core/agents/excel.agent.md dosyasını oku.
Tüm `<activation>`, `<workflow>`, `<rules>` bloklarını adım adım uygula.

Ticket ID: ${input:ticket}
