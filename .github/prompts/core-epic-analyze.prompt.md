---
agent: 'agent'
tools: ['search/codebase']
description: 'CORE — Epic analiz zinciri: tüm alt ticket\'ları kapsayan epic seviyesi analiz'
---

#file:.core/agents/orchestrator.agent.md dosyasını oku.
Dosyadaki `<epic-mode>` bloğunu etkinleştirerek `<activation>`, `<workflow>`, `<rules>` bloklarını uygula.

Talep (epic ticket ID veya --dry-run opsiyonu ile): ${input:talep}
