---
mode: agent
description: 'CORE — Epic analiz zinciri: Büyük kapsamlı taleplerde fazlı sprint planı + sub-story ayrıştırma'
---

#file:.core/agents/orchestrator.agent.md dosyasını oku.
`<epic-mode>` bloğunu aktif et. `<activation>`, `<workflow>`, `<rules>` bloklarını epic modunda uygula.

Epic talep: ${input:Epic ticket ID veya büyük kapsamlı talep metni}
