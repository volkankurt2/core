---
mode: 'agent'
tools: ['codebase', 'terminal']
description: 'CORE — Standart analiz zinciri: Jira ticket analizini orchestrator ile çalıştır'
---

#file:.core/agents/orchestrator.agent.md dosyasını oku.
Dosyadaki `<activation>`, `<workflow>`, `<rules>` bloklarını adım adım uygula.

Talep (ticket ID veya --dry-run opsiyonu ile): ${input:talep}
