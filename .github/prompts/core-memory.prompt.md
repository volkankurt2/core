---
mode: 'agent'
tools: ['codebase']
description: 'CORE — Kurumsal hafızayı sorgula: karar geçmişi, mimari notlar, politikalar'
---

#file:.core/agents/memory.agent.md dosyasını oku.
Tüm `<activation>`, `<workflow>`, `<rules>` bloklarını adım adım uygula.

Sorgu (opsiyonel — boş bırakılırsa genel özet): ${input:sorgu}
