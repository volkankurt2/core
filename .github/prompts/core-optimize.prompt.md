---
agent: 'agent'
tools: ['search/codebase']
description: 'CORE — Agent prompt optimizer: mevcut agent promptlarını iyileştir'
---

#file:.core/agents/prompt-optimizer.agent.md dosyasını oku.
Tüm `<activation>`, `<workflow>`, `<rules>` bloklarını adım adım uygula.

Hedef agent (opsiyonel — boş bırakılırsa tüm agentlar): ${input:hedef}
